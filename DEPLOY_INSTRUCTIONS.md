# تعليمات النشر السريع

## 📋 الملفات المُعدّة:

✅ تم تعديل `next.config.js` للتصدير الثابت
✅ تم إنشاء GitHub Actions workflow في `.github/workflows/deploy.yml`
✅ تم إضافة `.nojekyll` في مجلد public
✅ تم تحديث `package.json`

## 🚀 خطوات النشر:

### 1. ارفع التغييرات إلى GitHub:

```bash
git add .
git commit -m "Setup GitHub Pages deployment"
git push origin main
```

### 2. فعّل GitHub Pages:

1. اذهب إلى: https://github.com/Dakheel-code/Jud-APP/settings/pages
2. في قسم **Source**، اختر: **GitHub Actions**
3. سيبدأ النشر تلقائياً

### 3. الموقع سيكون متاحاً على:

```
https://dakheel-code.github.io/Jud-APP/
```

## ⏱️ وقت النشر:

- أول نشر: 3-5 دقائق
- التحديثات التالية: 2-3 دقائق

## 🔄 التحديثات التلقائية:

كل مرة تدفع تغييرات إلى branch `main`، سيتم النشر تلقائياً.

## ⚠️ ملاحظة مهمة:

هذا نشر ثابت (Static) - الميزات التي تتطلب خادم (API routes، قاعدة بيانات، مصادقة) لن تعمل.

للحصول على جميع الميزات، استخدم **Vercel** أو **Netlify**.

## 🐛 حل المشاكل:

إذا فشل النشر:
1. تحقق من Actions tab: https://github.com/Dakheel-code/Jud-APP/actions
2. اقرأ رسائل الخطأ
3. تأكد من أن جميع التبعيات موجودة في `package.json`
